version: '3.7'

services:
  cognihack:
    container_name: cognihack
    image: webgrip/cognihack
    ports:
    - 1337:8000
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    volumes:
      - .:/app